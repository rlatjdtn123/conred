<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
  
<mapper namespace="com.hk.conred.Like">
	
	<select id="likeList" parameterType="Map" resultType="LikeDto"> 
		SELECT  rn, cl.LIKE_LIST_SEQ ,cl.USER_ID ,cs.STORE_SEQ  ,cs.STORE_NAME,cs.STORE_INTRO_SIMPLE,cs.STORE_ADDRESS
		FROM
		    (SELECT  rownum AS rn,LIKE_LIST_SEQ ,USER_ID ,STORE_SEQ 
		     FROM CON_LIKE )cl,CON_STORE cs
		WHERE cl.store_seq=cs.STORE_SEQ AND CEIL(rn/5)=#{pnum} AND USER_ID =#{user_id}
	</select>
	
	
</mapper>
	  